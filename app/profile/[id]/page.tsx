import { getUserProjects } from "@/lib/actions"

type Props = {
    params: {
        id: string
    }
}

const UserProfile = () => {
  return (
    <div>UserProfile</div>
  )
}

export default UserProfile